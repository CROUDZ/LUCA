/**
 * SourceNode - Node source qui émet des signaux périodiquement
 *
 * Catégorie: Input
 *
 * Cette node ne possède pas d'entrée. Elle peut émettre des signaux à intervalle
 * régulier configuré. Utile comme point d'entrée pour générer un flot continu.
 */

// SourceNode removed — this file kept as a stub to avoid breaking imports in some workflows
// The node 'input.source' has been intentionally removed per user request and no
// longer registers itself with the NodeRegistry. If you need a source-like
// behavior, use 'input.trigger' or 'events.emitter' with a small helper.

export default undefined;
