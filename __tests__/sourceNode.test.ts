// Source node removed — tests referencing the old Source implementation have been removed.
// Keeping this file as a placeholder to avoid Jest import errors from other tests
// that referenced the test file name. If you need to re-add tests, create new
// ones that use 'input.trigger' and 'events.emitter' instead.

test('Source node removed — no tests', () => {
	// The Source node implementation has been removed from the codebase.
	// This placeholder ensures we don't leave a skipped test in the suite.
	expect(true).toBe(true);
});
