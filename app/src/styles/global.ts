/**
 * @deprecated Importer depuis '../theme' à la place
 *
 * Ce fichier est conservé pour la rétrocompatibilité.
 * Il réexporte la palette depuis le nouveau module src/theme/
 */

export { palette as basePalette } from '../theme/colors';
export type { ThemeColors as BasePalette } from '../theme/types';
