const React = require('react');
const { View } = require('react-native');
module.exports = function SocialMenuMock() {
  return React.createElement(View, { accessibilityLabel: 'social-menu-mock' });
};
